# Encargo T03 — Redactar el caso piloto

**Estado:** disponible  
**Responsable sugerido:** Contenido, con acceso local autorizado a las fuentes privadas  
**Dependencias satisfechas:** T01 y T02  
**Revisión de partida:** `15fab846f3a4ec922d7c4f96f3726f9a1d37be84`  
**Alcance:** trabajo local; no incluye commit, push, publicación ni despliegue.

## Objetivo

Redactar una primera versión revisable del caso sobre la evolución profesional dentro de García Delillo Construcciones. El texto debe servir luego como fuente única para la página del caso, el mapa, la navegación textual y los metadatos.

## Contexto que debe recibir el modelo

Leer antes de modificar:

- `AGENTS.md`;
- `docs/ejecucion/INICIO.md`;
- `docs/ejecucion/DECISIONES.md`;
- `docs/alcance_inicial.md`;
- `docs/ejecucion/TAREAS.md`, ficha T03;
- `docs/ejecucion/entregas/T02.md` y `T02-revision.md`;
- `referencias_privadas/01_Trayectoria_y_CV/matriz_garcia_delillo.md`, solo desde el entorno local autorizado;
- los documentos narrativos existentes que el integrador entregue como antecedentes, contrastándolos con la matriz.

La matriz privada es la fuente de trazabilidad. Los originales privados no se copian a la entrega ni se adjuntan a otro entorno automáticamente.

## Encargo

Escribir en español un relato claro y revisable que conecte las etapas de representación y dibujo, modelado y visualización, diseño y desarrollo de proyectos, y dirección o gestión de obras. Mantener la distinción entre:

- declaración curricular;
- hecho respaldado;
- interpretación editorial.

Las fechas, obras, herramientas, cifras, roles y resultados solo pueden aparecer si la matriz permite sostenerlos y su autorización está registrada. Las dudas deben quedar marcadas como pendientes; no completar vacíos por cálculo o inferencia. No atribuir obras concretas a García Delillo por la mera presencia de una imagen en una carpeta privada.

El relato debe incluir un título, una introducción vinculada al eje «El output cambia. El método permanece.», etapas con capacidades aplicadas y una sección breve de fuentes o notas editoriales que pueda trasladarse a HTML. No fijar todavía decisiones de navegación, componentes o URLs que correspondan a T05.

## Archivos permitidos

Puede crear o modificar únicamente:

- `docs/casos/garcia-delillo.md`;
- `docs/ejecucion/entregas/T03.md`.

No modificar `TAREAS.md`, `DECISIONES.md`, originales privados, `public/`, `src/` ni otros casos. Si aparece una decisión funcional, una autorización faltante o una contradicción de fuentes, registrarla en el informe y detener solo la parte afectada.

## Entrega y comprobaciones

Devolver el archivo del caso y un informe siguiendo `docs/ejecucion/ENTREGA.md`. Comprobar realmente:

1. cada afirmación relevante se puede relacionar con un identificador de la matriz;
2. los hechos, declaraciones e interpretaciones están diferenciados;
3. no hay datos privados innecesarios ni materiales de evidencia incorporados;
4. el Markdown se puede leer sin componentes ni JavaScript;
5. las fechas y atribuciones abiertas aparecen como pendientes;
6. el informe separa cambio local, commit, push, despliegue y URL.

## Cambios locales recibidos

La base de Git no contiene todavía un commit con el kit de ejecución. Al recibir el encargo, confirmar `git status --short --untracked-files=all`. En la preparación actual existen cambios en `README.md` y `docs/proyecto_aplicacion.md`, además de documentos sin seguimiento en `docs/`, `docs/ejecucion/`, `docs/ejecucion/entregas/` y este directorio de encargos. La matriz privada está excluida por `.gitignore` y debe permanecer fuera de la entrega pública.

## Siguiente paso

Devolver el caso y el informe al integrador. El integrador revisará T03 antes de habilitar T04 y de integrar el relato con la arquitectura de T05.
