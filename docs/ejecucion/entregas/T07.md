# Entrega T07 — Base técnica

**Tarea:** T07  
**Ejecutor:** Codex / Desarrollo local  
**Fecha:** 2026-09-06  
**Estado propuesto:** `completa`

## Resultado

Se preparó una aplicación Astro estática con TypeScript, React y React Flow. La aplicación lee el contenido editorial local, genera rutas directas y conserva una navegación HTML para personas y buscadores.

## Base creada

- `package.json` y `package-lock.json` con scripts `dev`, `check`, `build` y `preview`.
- `astro.config.mjs` con la integración de React.
- `tsconfig.json` en modo estricto de Astro.
- `src/data/portfolio.ts` para el contrato de nodos, conexiones, etapas, capacidades y proyectos.
- `src/data/evidence.ts` para leer el manifiesto de evidencias autorizado.
- `src/data/project-content.ts` para reutilizar los textos de proyecto aprobados.
- Rutas Astro para inicio, caso, proyectos, capacidades y trayectoria.
- `public/evidencias/garcia-delillo/` con el manifiesto y sus 102 recursos ya preparados en T04.

## Versiones declaradas

Las versiones exactas quedan fijadas en `package-lock.json`. El conjunto instalado incluye Astro, `@astrojs/react`, React, React DOM, `@xyflow/react`, `@dagrejs/dagre`, TypeScript y `shiki`.

## Criterios de cierre

- La instalación local se completó.
- Astro generó el sitio estático sin errores.
- La ruta inicial tiene HTML esencial antes de la interacción del mapa.
- El caso y sus proyectos tienen URLs directas.
- Las imágenes se sirven desde `public/` y el contenido privado continúa en `referencias_privadas/`, excluido de Git.
- No se hizo commit, push, despliegue ni publicación.

## Comprobaciones

- `npm.cmd run check`: 0 errores, 0 advertencias y 0 avisos.
- `npm.cmd run build`: 18 páginas estáticas generadas correctamente.

## Estado de publicación

- Cambio local: realizado.
- Commit: no creado.
- Push: no realizado.
- Despliegue: no realizado.
- URL pública: no comprobada.
