# Entrega T12 — Contacto y perfil público

**Tarea:** T12
**Ejecutor:** Codex / Integración local
**Fecha:** 2026-09-06
**Estado propuesto:** `completa`

## Resultado

Se incorporó la ruta `/contacto/` con el perfil individual de GitHub de RamaReid como canal de contacto. La página explica el uso profesional del perfil, enlaza directamente al destino y conserva el contenido esencial en HTML.

No se agregaron correo electrónico, teléfono ni formulario. Esos canales quedan pendientes de una decisión posterior si se consideran necesarios.

## Cambios realizados

- `src/pages/contacto/index.astro`: página estática de contacto y perfil con JSON-LD.
- `src/layouts/BaseLayout.astro`: enlace Contacto en la navegación y en el pie.
- `docs/contacto.md`: decisión y límites del canal aprobado.
- `docs/ejecucion/DECISIONES.md`: D13 cerrada con GitHub como perfil y canal.
- `docs/ejecucion/TAREAS.md`: T12 actualizada como completa.

## Validación realizada

- El perfil remoto `https://github.com/RamaReid` respondió HTTP 200.
- `npm.cmd run check`: 0 errores, 0 advertencias y 0 avisos.
- `npm.cmd run build`: correcto; se generó la ruta `/contacto/` junto con el resto del sitio.
- La ruta local `/contacto/` respondió HTTP 200.
- La página contiene JSON-LD, el enlace de GitHub y texto visible sin JavaScript.
- `git diff --check`: sin errores.

## Estado de publicación

- Cambio local: realizado.
- Commit: se crea localmente con la entrega revisada.
- Push: no realizado.
- Despliegue: no realizado.
- URL pública del portfolio: no comprobada.
- URL externa de contacto: comprobada, HTTP 200.

