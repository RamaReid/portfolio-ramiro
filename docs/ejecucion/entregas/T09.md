# Entrega T09 — Caso Productoria / Genio Productor

**Tarea:** T09
**Ejecutor:** Codex / Desarrollo e integración local
**Fecha:** 2026-09-06
**Estado propuesto:** `completa`

## Resultado

Se incorporó el caso Productoria / Genio Productor al portfolio. La ruta `/casos/productoria/` explica el prototipo de entrevista conversacional y mapa organizacional, enlaza la fuente de GitHub y mantiene fuera del frontend las conversaciones, el código y los datos privados.

El explorador ahora contiene dos casos y cuatro capacidades nuevas: arquitectura conversacional, modelado de sistemas, diagnóstico organizacional y diseño de workflows. Las conexiones del caso aparecen en el mapa y en la navegación textual inicial.

## Cambios realizados

- `src/data/portfolio.ts`: tipos de caso y relación de casos, capacidades, etapas y proyectos; nuevo caso Productoria y conexiones.
- `src/pages/casos/productoria/index.astro`: página estática con relato, flujo, decisiones técnicas, límites, fuentes y JSON-LD.
- `src/pages/index.astro`: casos y relaciones nuevas en el fallback HTML y tarjetas de casos.
- `src/pages/capacidades/[slug].astro`: capacidades sin etapa temporal y enlaces a los casos relacionados.
- `src/layouts/BaseLayout.astro`: enlace directo al nuevo caso.
- `docs/casos/productoria.md`: ficha editorial pública con matriz resumida de afirmaciones.
- `docs/inventario_maestro.md` y `docs/plan_desarrollo.md`: F04 actualizado con la fuente correcta.
- `referencias_privadas/03_Productoria/`: copia local del repositorio, metadata y matriz privada; permanece excluida de Git.

## Validación realizada

- `npm.cmd run check`: 0 errores, 0 advertencias y 0 avisos.
- `npm.cmd run build`: correcto; 23 páginas estáticas generadas.
- Previsualización local en `http://localhost:4321` y HTTP 200 para `/`, `/casos/productoria/`, `/casos/garcia-delillo/`, `/capacidades/arquitectura-conversacional/`, `/capacidades/modelado-de-sistemas/` y `/trayectoria/`.
- La página de Productoria contiene JSON-LD y el enlace al repositorio fuente; la página de García Delillo no muestra capacidades de Productoria.
- Las respuestas verificadas no contienen `referencias_privadas`; el caso Productoria no contiene código, conversaciones, credenciales ni datos de usuarios.
- `git diff --check`: sin errores de diff; Git solo informó conversiones de fin de línea LF/CRLF.

## Estado de publicación

- Cambio local: realizado.
- Commit: se crea localmente con la entrega revisada.
- Push: no realizado.
- Despliegue: no realizado.
- URL pública: no comprobada.
