# Entrega T08 — Primer recorrido completo

**Tarea:** T08
**Ejecutor:** Codex / Desarrollo e integración local
**Fecha:** 2026-09-06
**Estado propuesto:** `completa`

## Resultado

Se integró el recorrido completo del piloto en Astro. El mapa comparte sus nodos y conexiones con una navegación textual HTML. Al seleccionar un nodo, el panel muestra su descripción, enlace directo y la explicación de cada conexión relacionada.

La página inicial enlaza el caso, las etapas, las capacidades y los diez proyectos. El caso enlaza cada proyecto y muestra recursos visuales autorizados. Cada proyecto conserva su relato textual, galería, texto alternativo y metadatos `CreativeWork`.

## Cambios realizados

- `src/components/Explorer.tsx`: listado textual de conexiones en el detalle del nodo seleccionado.
- `src/pages/index.astro`: listado HTML de las relaciones del grafo, con enlaces a origen y destino.
- `src/styles/global.css`: estilos responsive para las relaciones textuales y el detalle.
- `docs/ejecucion/encargos/T08.md`: encargo reproducible y criterios de cierre.
- `docs/ejecucion/TAREAS.md`: T08 marcada como completa y tareas posteriores habilitadas según dependencias.

## Validación realizada

- `npm.cmd run check`: 0 errores, 0 advertencias y 0 avisos.
- `npm.cmd run build`: correcto; 18 páginas estáticas generadas.
- `npm.cmd run preview -- --host localhost --port 4321 --strictPort`: servidor de previsualización iniciado.
- Comprobación HTTP local de `/`, `/casos/garcia-delillo/`, `/proyectos/garcia-delillo/cedahause/`, `/capacidades/representacion/` y `/trayectoria/`: las cinco devolvieron `200`.
- Las respuestas contienen enlaces y texto estático del caso; el inicio contiene las relaciones explicadas.
- El caso y el proyecto contienen `application/ld+json`.
- Las respuestas verificadas no contienen `referencias_privadas`.
- El manifiesto conserva 10 proyectos, 102 recursos, 0 faltantes y 0 textos alternativos vacíos.
- La revisión de implementación confirma `nodesFocusable`, `edgesFocusable`, controles con texto, foco visible, distribución responsive y detalle fuera del mapa.

## Límites de la comprobación

La validación se realizó localmente con salida estática y previsualización HTTP. No se realizó todavía una prueba con URL pública, navegador automatizado, lector de pantalla real ni dispositivo móvil físico.

## Estado de publicación

- Cambio local: realizado.
- Commit: incluido en el commit local de T08.
- Push: no realizado.
- Despliegue: no realizado.
- URL pública: no comprobada.
