# Entrega T13 — Validación integral local

**Tarea:** T13
**Ejecutor:** Codex / Desarrollo e integración local
**Fecha:** 2026-09-06
**Estado:** `completa`, con observación de rendimiento

## Resultado

La versión integral local compila y responde correctamente en todas sus rutas generadas. El contenido esencial, los enlaces internos, el contacto, los casos, la trayectoria y las capacidades están disponibles en HTML estático.

## Comprobaciones ejecutadas

| Área | Comando o método | Resultado |
|---|---|---|
| Tipos y Astro | `npm.cmd run check` | 0 errores, 0 advertencias y 0 avisos |
| Generación estática | `npm.cmd run build` | 31 páginas generadas correctamente |
| Rutas | Previsualización en `http://localhost:4321` | 31 rutas `index.html` con HTTP 200 |
| Enlaces internos | Extracción de `href` y petición local | 33 enlaces únicos con HTTP 200 |
| HTML base | Revisión de 31 documentos | Todos tienen `lang="es"`, descripción y `h1` |
| Fallback | `dist/index.html` | Incluye los tres casos, contacto, lista textual y relaciones explicadas |
| Contacto | `dist/contacto/index.html` | Incluye GitHub, JSON-LD y no contiene `mailto:` |
| Metadatos | Casos y contacto | JSON-LD presente y coherente con cada página |
| Privacidad | Búsqueda en `dist/` | Sin `referencias_privadas` ni `.env` |
| Accesibilidad estructural | Revisión de código y HTML | `lang`, skip link, foco visible, botones con texto, `aria-label`, `aria-live` y nodos enfocables presentes |
| Responsive | Revisión de CSS y componentes | Cortes a 820/560 px, detalle fuera del mapa y `prefers-reduced-motion` presentes |
| Diff | `git diff --check` | Sin errores |

## Observación de rendimiento

La salida contiene 140 archivos y pesa 232.456.184 bytes. Las evidencias visuales autorizadas incluyen imágenes individuales de varios megabytes. El sitio funciona localmente, pero este peso debe revisarse antes de una publicación pública mediante compresión, dimensiones adecuadas o una selección de medios; la optimización no se declara realizada en T13.

No se ejecutó Lighthouse ni una medición de red en un navegador real. La comprobación de rendimiento queda limitada al peso de la salida y a la compilación estática; T14 debe revisar si el riesgo bloquea la publicación.

## Límites y estado de publicación

- Cambio local: validado.
- Commit: creado localmente para esta entrega.
- Push: no realizado.
- Despliegue: no realizado.
- URL pública del portfolio: no comprobada.
- URL externa de contacto: comprobada previamente, HTTP 200.
