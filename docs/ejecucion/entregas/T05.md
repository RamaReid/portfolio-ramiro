# Entrega T05 — Arquitectura de contenido

**Tarea:** T05  
**Ejecutor:** Codex / Arquitectura de contenido  
**Fecha:** 2026-09-06  
**Rama:** `main`  
**Commit de partida:** `15fab846f3a4ec922d7c4f96f3726f9a1d37be84`  
**Estado propuesto:** `completa`

## Resultado

Se creó `docs/arquitectura_contenido.md` con el contrato común para casos, proyectos, capacidades, etapas, evidencias, conexiones, eventos temporales, páginas, URLs, HTML sin JavaScript y metadatos.

El contrato se adapta al piloto actual: un caso de García Delillo, diez proyectos textuales y 102 imágenes con manifiesto. Mantiene separadas las fuentes privadas y define cómo el mapa y la navegación textual deben consumir la misma colección editorial.

## Decisiones y propuestas incluidas

- Se conserva HTML esencial antes de la hidratación del mapa.
- Se definen identificadores estables y slugs directos.
- Se separan `publicText`, tipo de afirmación, fuente y estado editorial.
- Se establece el contrato mínimo de una evidencia con `src`, `alt`, procedencia, página de origen, tipo y estado.
- Se exige que conexiones y metadatos reutilicen texto visible.
- Se mantiene un caso sin evidencias como estado válido del contrato.
- Se dejan T06, T07 y T08 como responsables de diseño visual, tipos reales y validación de la integración.

## Comprobaciones realizadas

- El documento describe una única fuente para mapa, HTML y páginas.
- Incluye rutas directas para caso, proyecto, capacidad, trayectoria y evidencia.
- Incluye fallback textual y comportamiento sin JavaScript.
- Separa campos privados de campos que pueden llegar al frontend.
- Define la coincidencia entre HTML, metadatos y JSON-LD.
- No agrega fechas, métricas, roles, clientes ni resultados nuevos al piloto.
- El manifiesto local contiene 10 proyectos y 102 recursos; todos tienen archivo existente y texto alternativo.

## Cambios y publicación

- **Cambio local:** creado `docs/arquitectura_contenido.md` y este informe; ajustado el manifiesto para usar el estado `approved` por recurso.
- **Commit:** no creado.
- **Push:** no realizado.
- **Despliegue:** no realizado.
- **Frontend y URL:** no comprobados; la aplicación todavía no existe.
- **Migraciones o funciones de servidor:** no aplican.
